
SQL: 
    Location:
        Agregar el campo fecha [X]
        Agregar el id del usuario
    User:
        Agregar usuario

Server:
    Correr un cron para limpiar las locaciones menores a 1 semana

BackEnd:
    Location - GET:
        Limitar el GET de locations
    User - POST:
        Agregar usuario
    User - GET:
        Loggin usuario

FrontEnd: 
    Index:
        Cambiar el color y animacion del ultimo punto donde se hizo el request POST location [X]
        Agregar un HINT de como usar la app
        Agregar la fecha del request en el marker https://developers.google.com/maps/documentation/javascript/examples/infowindow-simple

create table users (id int not null auto_increment, username varchar(25) not null, password varchar(25) not null, foreign key (id) references user_location(user_id),primary key (id));
alter table user_location add user_id int not null;